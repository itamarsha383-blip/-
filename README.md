# 🤖 מערכת סוכני AI לעסק אונליין בשוק האמריקאי

מערכת קטנה ועובדת של "סוכני AI" שעושים בשבילך מחקר שוק אמיתי על השוק האמריקאי,
מחפשים ספקים בסין, מחשבים רווחיות, וכותבים לך תוכן שיווקי מוכן —
**בלי שתצטרך אנגלית** (הסוכנים כותבים את האנגלית ומסבירים לך הכל בעברית).

המערכת בנויה כמו צוות קטן:

| סוכן | תפקיד |
|------|--------|
| 🧑‍💼 **מנכ"ל** | מפרק מטרה עסקית למשימות מחקר ומסכם דוח החלטות |
| 🔍 **חוקרים** | מחפשים נתונים אמיתיים: מחירים, מתחרים, ביקוש |
| 📦 **ספקים** | מחפשים ב-Alibaba/AliExpress, מחשבים עלות נחיתה לפריט |
| 💰 **כלכלן** | מחשב עלויות, תמחור ורווחיות למכירה אונליין |
| ✍️ **שיווק** | כותב תוכן שיווקי **באנגלית** לשוק האמריקאי + הסבר בעברית |

---

## ⚠️ קודם — האמת בלי כחל ושרק

- זו **לא מכונת כסף**. היא כלי מחקר שעוזר לך להחליט במה כדאי להשקיע.
- כדי להרוויח כסף עדיין צריך מוצר, לקוחות וזמן. שום סוכן לא עושה את זה בלחיצת כפתור.
- מה שהמערכת **כן** נותנת: חיסכון של שעות מחקר, ונתונים אמיתיים שעוזרים לבחור נכון.

---

## 📋 מה צריך לפני שמתחילים (פעם אחת)

1. **מחשב** עם חיבור לאינטרנט.
2. **Python** מותקן. בדיקה: פתח טרמינל/שורת פקודה והקלד `python3 --version`.
   אם אין — הורד מ-https://python.org (לחץ Download, התקן, סמן "Add to PATH").
3. **מפתח API של Anthropic**:
   - היכנס ל-https://console.anthropic.com
   - הירשם (חינם), ואז **Billing → הוסף קצת קרדיט** (אפילו $5 מספיק להמון בדיקות).
   - לחץ **API Keys → Create Key**, והעתק את המפתח (מתחיל ב-`sk-ant-`).
   - ⚠️ שמור אותו במקום בטוח — אל תשתף אותו עם אף אחד.

---

## 🌐 הפעלה דרך הדפדפן (ממשק ווב — הכי נוח!)

**שלב 1 — התקן את כל הספריות (פעם אחת):**
```
pip install -r requirements.txt
```

**שלב 2 — הכנס את מפתח ה-API:**

ב-Mac / Linux:
```
export ANTHROPIC_API_KEY=sk-ant-הכנס-כאן-את-המפתח-שלך
```
ב-Windows:
```
set ANTHROPIC_API_KEY=sk-ant-הכנס-כאן-את-המפתח-שלך
```

**שלב 3 — הפעל את השרת:**
```
python3 app.py
```

**שלב 4 — פתח בדפדפן:**
```
http://localhost:5000
```

הקלד את התחום, לחץ **"התחל מחקר"**, ותראה את ההתקדמות בזמן אמת.
בסוף — לחץ **"הורד קובץ Word"** לקובץ מעוצב שאפשר לפתוח ב-Word/LibreOffice.

---

## 🖥️ הפעלה דרך שורת הפקודה (אלטרנטיבה)

```
python3 agents.py
```
המערכת תשאל אותך מה לחקור. הקלד תחום עסקי, לדוגמה:
> מכירת מוצרי טיפוח טבעיים אונליין בארה"ב

או שתעביר את התחום ישר בפקודה:
```
python3 agents.py "מכירת בקבוקי מים חכמים בארה\"ב"
```

---

## 🧠 איזה מודל לבחור (חשוב!)

המערכת רצה כברירת מחדל על **Sonnet 4.6** — האיזון הכי טוב בין איכות למחיר.
אפשר לבחור מודל אחר **בלי לגעת בקוד**, דרך משתנה סביבה:

| מודל | מחיר (קלט/פלט ל-1M טוקנים) | מתי |
|------|-----------------------------|-----|
| `claude-haiku-4-5` | $1 / $5 | הכי זול ומהיר, פחות מעמיק |
| `claude-sonnet-4-6` | $3 / $15 | ⭐ ברירת מחדל — האיזון הכי טוב |
| `claude-opus-4-8` | $5 / $25 | הכי חכם, להחלטות גדולות |

איך בוחרים (לפני שמפעילים):
```
Mac/Linux:   export AGENT_MODEL=claude-opus-4-8
Windows:     set AGENT_MODEL=claude-opus-4-8
```

---

## 💡 טיפים

- **רוצה תוצאות שונות?** פשוט הרץ שוב עם תחום אחר.
- **רוצה יותר/פחות עומק?** פתח את `agents.py` ושנה את `NUM_RESEARCH_TASKS` (ברירת מחדל 4).
- **התוכן השיווקי באנגלית** מוכן להעתקה ישירה לדף מכירה / אמזון / מודעה.
  לצד כל חלק יש הסבר בעברית כדי שתבין מה כתוב.
- **קובץ Word** — אחרי ההרצה נשמר אוטומטית קובץ `report_<תאריך>.docx` שאפשר לפתוח ישירות.
- **כמה זה עולה?** כל הרצה עולה בדרך כלל סנטים בודדים עד כמה עשרות סנט,
  תלוי במודל ובכמות החיפושים. עקוב אחרי השימוש ב-console.anthropic.com.

---

## ❓ בעיות נפוצות

| בעיה | פתרון |
|------|--------|
| `command not found: python3` | נסה `python` במקום `python3` |
| `לא נמצא מפתח API` | חזור על שלב 2 — צריך להגדיר את המפתח בכל פעם שפותחים טרמינל חדש |
| `pip: command not found` | נסה `python3 -m pip install -r requirements.txt` |
| הדפדפן מראה שגיאה על מפתח API | הגדר את `ANTHROPIC_API_KEY` **לפני** שמפעילים את `app.py` |
| המערכת איטית | זה תקין — החוקרים מחפשים באינטרנט. תן לה כמה דקות |
| קובץ Word לא נשמר | ודא שהתקנת את כל הספריות: `pip install -r requirements.txt` |

---

## 📂 מבנה הפרויקט

```
agents.py          ← הקוד הראשי (כל הסוכנים + לוגיקה)
app.py             ← שרת הווב (הממשק הגרפי בדפדפן)
templates/
  index.html       ← דף הווב
requirements.txt   ← הספריות הדרושות
.env.example       ← דוגמה לקובץ עם מפתח API
README.md          ← המדריך הזה
```
