/* ============================================================================
   Novixel — public site config
   Same Supabase keys as admin/config.js. When filled in, the public site
   loads any content you edited in the admin panel. When empty, the site
   shows its built-in default text (so it always works).
   ============================================================================ */
window.NOVIXEL_CONFIG = {
  SUPABASE_URL: "",
  SUPABASE_ANON_KEY: ""
};
